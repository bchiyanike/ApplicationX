<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Theme.AlphaPlayer" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>

        <!-- This fixes the drawerLayoutStyle error -->
        <item name="drawerLayoutStyle">@style/Widget.AlphaPlayer.DrawerLayout</item>
    </style>

    <!-- Style for DrawerLayout -->
    <style name="Widget.AlphaPlayer.DrawerLayout" parent="Widget.MaterialComponents.DrawerLayout">
        <!-- Customize your drawer style here if needed -->
    </style>
</resources>
